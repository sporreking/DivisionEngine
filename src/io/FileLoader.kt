package io

/** Used for loading files. */
class FileLoader {
    companion object {

        /** Loads the contents of the specified [path] as a string. */
        fun loadSource(path: String) {

        }
    }
}